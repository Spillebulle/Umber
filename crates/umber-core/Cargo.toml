[package]
name = "umber-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
glam = { workspace = true }
bytemuck = { workspace = true }

# `docimport` only. Data-format crates, no GPU or windowing types, so the
# crate boundary in CLAUDE.md still holds.
zip = { workspace = true }
png = { workspace = true }
quick-xml = { workspace = true }
psd = { workspace = true }
